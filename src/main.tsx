import React from 'react';
import OtogeMap from './components/OtogeMap';
import { Outlet } from 'react-router';

export const Main = () => {

  return <div>
    <header>
  aaa
    </header>
    <main>
    <Outlet />
    </main>
    <footer>

    </footer>
  </div>
}